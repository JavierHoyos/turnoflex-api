spring.application.name=turnoflex-api

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/turnoflex_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:wer1234}
spring.datasource.driver-class-name=org.postgresql.Driver

mybatis.type-aliases-package=com.turnoflex.turnoflex_api.model

server.port=${PORT:8080}